import{access,readFile}from'node:fs/promises';import{fileURLToPath}from'node:url';const routes=[['/','Calypso Digital Studio'],['/services','Services | Calypso Digital Studio'],['/work','Work | Calypso Digital Studio'],['/about','About | Calypso Digital Studio'],['/process','Process | Calypso Digital Studio'],['/start','Start Your Project | Calypso Digital Studio']];const errors=[];for(const[route,title]of routes){const name=route==='/'?'../dist/index.html':`../dist${route}/index.html`;const file=fileURLToPath(new URL(name,import.meta.url));try{const html=await readFile(file,'utf8');if(!html.includes(`<title>${title}</title>`))errors.push(`${route}: missing expected title`);if(!html.includes('name="description"'))errors.push(`${route}: missing meta description`);if(!html.includes('id="main-content"'))errors.push(`${route}: missing main-content target`)}catch{errors.push(`${route}: build output missing`)}}for(const f of['favicon.svg','site.webmanifest','robots.txt'])try{await access(fileURLToPath(new URL(`../dist/${f}`,import.meta.url)))}catch{errors.push(`/${f}: missing from build`)}if(errors.length){console.error(errors.join('\n'));process.exitCode=1}else console.log(`Verified ${routes.length} routes and public metadata files.`);
