import assert from'node:assert/strict';import test from'node:test';import{validateContent}from'./verify-content-config.mjs';test('foundation content is valid',async()=>{const result=await validateContent();assert.deepEqual(result.errors,[]);assert.equal(result.projectCount,3)});test('Good Intentions remains explicitly in progress',async()=>{const result=await validateContent();const project=result.projects.find((entry)=>entry.slug==='good-intentions');assert.equal(project?.status,'in-progress');assert.equal(project?.liveUrl,undefined)});
